apiVersion: apps/v1
kind: Deployment
metadata:
  name: tickets-mongo-depl
spec:
  replicas: 1
  selector:    # find pods how it acutually created
    matchLabels:
      app: tickets-mongo

  template:
    metadata:
      labels:  # label that get apply to the pod
        app: tickets-mongo
    spec:
      containers:
        - name: tickets-mongo
          image: mongo   # fetch from docker hub
---

apiVersion: v1
kind: Service
metadata:
  name: mongo-tickets-srv
spec:
  selector:
    app: tickets-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017